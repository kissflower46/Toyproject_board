CREATE TABLE `study`.`board` ( `board_no` INT(8) NOT NULL, `board_title` VARCHAR(128), `board_contents` VARCHAR(1000), `board_date` DATE, PRIMARY KEY (`board_no`) ) CHARSET=utf8; 

--drop TABLE board;

